apiVersion: apps/v1
kind: Deployment
metadata:
  name: withus-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: withus-staging
  template:
    metadata:
      labels:
        app: withus-staging
    spec:
      containers:
        - name: withus
          image: shash042319/withus:b868961
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
