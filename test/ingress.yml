apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: withus-test-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: test.jk-project.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: withus-test-service  # Service의 이름!
                port:
                  number: 80
